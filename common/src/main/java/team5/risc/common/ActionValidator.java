package team5.risc.common;

public class ActionValidator {
  public Boolean isValid(MoveAction a, Map map) {
    return true;
  }

  public Boolean isValid(AttackAction a, Map map) {
    return true;
  }
}
